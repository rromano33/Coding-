__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/

# Dados locais — planilha real do usuário e caches, nunca versionados
Data/Inputs_BCs.xlsx
Data/raw/*
Data/processed/*
!Data/raw/.gitkeep
!Data/processed/.gitkeep

# Config pessoal de cada trader (caminho/aba/colunas da própria planilha de
# portfólio) — nunca versionado, pra compartilhar o código do VaR com a mesa
# sem o config de um trader sobrescrever o de outro. Template compartilhado
# fica em config/portfolio_risk.example.yaml (esse sim versionado).
config/portfolio_risk.yaml

# Idem, pro book de FX trend/carry (aba "Preços FX" do mesmo Portfolio.xlsx).
# Template compartilhado em config/fx_strategy.example.yaml.
config/fx_strategy.yaml

